# Cross-platform dependencies for price comparison system
# No external dependencies required - uses only Python standard library (tkinter)
# For enhanced features and better cross-platform compatibility:

# Optional: Better CSV handling with Excel compatibility
openpyxl>=3.1.0

# Qt-based modern desktop UI
PySide6>=6.6.0

# Optional: System tray integration (platform-specific)
pystray>=0.19.4

# Optional: Better font handling
pillow>=10.0.0

# For creating executables (development dependencies)
pyinstaller>=5.0.0
